// import { createReducer, createSlice } from '@reduxjs/toolkit'

const initialState = {'dummy2': 'dummy2'}

const dummyReducer2 = (state = initialState, {type, payload}) =>{
   return state
}

export default dummyReducer2